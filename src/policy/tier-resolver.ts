/**
 * Tier classification: resolves an entity ID and action to a policy tier number.
 * Follows the resolution order: entity overrides > tier entity lists > domain patterns > default.
 *
 * Implementation: Phase 2
 */

export {};
